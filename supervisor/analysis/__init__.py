"""Supervisor post-hoc analysis plane (off the deterministic hot path).

Reflection-time analysis that turns reconciled journal / exit / gate evidence into aggregate,
distribution-level findings the research loop (§56) can act on. Nothing here touches the Rust
decision path; it reads evidence and produces distributions, never anecdotes (§56.5).
"""
