//! pump-quant-evaluator — workspace crate. Modules below are filled in leaf-by-leaf by the build;
//! each module's functions are implemented against locked dossier property tests.

pub mod evaluator_stats;
