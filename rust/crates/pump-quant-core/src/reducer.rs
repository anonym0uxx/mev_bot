//! reducer — implemented leaf-by-leaf against the dossier property tests.
//! Functions are added here by the build; this skeleton only establishes the module.

// module scaffold marker (keeps the file valid before leaves land)
#[allow(dead_code)]
const _MODULE_SCAFFOLD: () = ();
