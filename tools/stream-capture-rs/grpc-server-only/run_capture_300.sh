#!/bin/bash
# run_capture_300.sh — superseded by run_capture.sh (creds-safe).
# Kept as a 300-min alias. The old hardcoded API key has been removed.
exec /mnt/d/repos/mev_bot/tools/stream-capture-rs/grpc-server-only/run_capture.sh 300