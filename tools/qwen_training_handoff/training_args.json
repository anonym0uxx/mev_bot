{
  "model_name_or_path": "Qwen/Qwen2.5-27B",
  "training_phase": "CPT",
  "output_dir": "./output/qwen_cpt",
  "num_train_epochs": 3,
  "per_device_train_batch_size": 2,
  "gradient_accumulation_steps": 16,
  "learning_rate": 5e-5,
  "lr_scheduler_type": "cosine",
  "warmup_ratio": 0.05,
  "weight_decay": 0.01,
  "max_grad_norm": 1.0,
  "bf16": true,
  "gradient_checkpointing": true,
  "logging_steps": 10,
  "save_steps": 200,
  "save_total_limit": 5,
  "save_strategy": "steps",
  "seed": 42,
  "data_seed": 42,
  "dataloader_num_workers": 4,
  "dataloader_pin_memory": true,
  "report_to": "none",
  "remove_unused_columns": false,
  "max_seq_length": 4096,
  "packing": true,
  "dataset_text_field": "text",
  "_comment": "Phase 1 = CPT on qwen_cpt_v1 (~34M tokens). Phase 2 = SFT on qwen_sft_v1 (~15M tokens). Change training_phase + lr + output_dir for SFT.",
  "_sft_overrides": {
    "training_phase": "SFT",
    "output_dir": "./output/qwen_sft",
    "learning_rate": 1e-5,
    "num_train_epochs": 2
  }
}
